export function toUpperCaseArray(items: string[]): string[] {
    return items.map((item: string) => item.toUpperCase());
}

export function toLowerCaseArray(items: string[]): string[] {
    return items.map((item: string) => item.toLowerCase());
}
